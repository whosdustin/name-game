development:
  clients:
    default:
      database: namegame_dev
      hosts:
        - localhost:27017
  options:

production:
  clients:
    default:
      uri: <%= ENV['MONGODB_URI'] %>
      options:
